import React from 'react';
import { View, TextField } from 'react-native';

const Input = () => {
    return (
        <View>
            <TextField />
        </View>
    );
};

export default Input;